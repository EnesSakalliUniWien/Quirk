/** Shared border widths and corner radii in logical pixels. */
export const Borders = Object.freeze({
  width: Object.freeze({ regular: 1, strong: 2, focus: 3 }),
  // tile: the corner every gate, display and readout on the circuit wears.
  radius: Object.freeze({ small: 4, tile: 6, medium: 8, control: 10 }),
});
